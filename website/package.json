{
  "name": "ax-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun gen.ts && wrangler dev",
    "deploy": "bun gen.ts && wrangler deploy",
    "format": "oxfmt --check .",
    "format:fix": "oxfmt --write ."
  },
  "dependencies": {
    "hono": "^4.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "wrangler": "^4.0.0"
  }
}
